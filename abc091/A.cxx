#include <cstdio>

using namespace std;

#define REP(i,n)   for(int i=0; i<(int)(n); i++)
#define FOR(i,b,e) for(int i=(b); i<=(int)(e); i++)

const int B_MAX = 500;
const int C_MAX = 1000;

int A, B, C;

void solve() {
  int ans = 0;
  printf("%d\n", ans);
}

void input() {
  scanf("%d%d%d", &A, &B, &C);
}

int main() {
  input();
  solve();
  return 0;
}
