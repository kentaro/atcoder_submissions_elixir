#include <cstdio>

using namespace std;

#define REP(i,n)   for(int i=0; i<(int)(n); i++)
#define FOR(i,b,e) for(int i=(b); i<=(int)(e); i++)

const int OE_MAX = 50;

char O[O_MAX + 1];
char E[E_MAX + 1];

void solve() {
  int ans = 0;
  printf("%d\n", ans);
}

void input() {
  scanf("%s", O);
  scanf("%s", E);
}

int main() {
  input();
  solve();
  return 0;
}
