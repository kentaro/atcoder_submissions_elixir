#include <cstdio>

using namespace std;

#define REP(i,n)   for(int i=0; i<(int)(n); i++)
#define FOR(i,b,e) for(int i=(b); i<=(int)(e); i++)

const int B_MAX = 1000000000000000000;
const int CD_MAX = 1000000000;

int A, B, C, D;

void solve() {
  int ans = 0;
  printf("%d\n", ans);
}

void input() {
  scanf("%d%d%d%d", &A, &B, &C, &D);
}

int main() {
  input();
  solve();
  return 0;
}
